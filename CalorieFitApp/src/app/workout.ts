export interface Workout {
    id: number;             // Unique identifier
    workout: string;        // Name of the workout
    muscleGroup: string;    // Targeted muscle group
    day: string;            // Day of the workout
    instructions: string;   // Workout instructions
  }
  